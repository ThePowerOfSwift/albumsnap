query AllPhotos($last: Int) {
    photos: allPhotos(last: $last) {
        ...PhotoDetails
    }
}

fragment PhotoDetails on Photo {
    id
    name
    caption
    createdAt
    file {
        id
        url
    }
    album {
        id
        name
    }
}
